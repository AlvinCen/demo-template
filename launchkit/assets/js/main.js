(function(){
  const html=document.documentElement;const saved=localStorage.getItem('launchkit-theme');if(saved==='dark')html.classList.add('dark');
  document.addEventListener('click',e=>{const t=e.target.closest('[data-theme-toggle]');if(t){html.classList.toggle('dark');localStorage.setItem('launchkit-theme',html.classList.contains('dark')?'dark':'light');}});
  document.addEventListener('click',e=>{const t=e.target.closest('.tab');if(!t)return;const tabs=t.parentElement.querySelectorAll('.tab');const panels=t.parentElement.nextElementSibling.querySelectorAll('.tabpanel');tabs.forEach((tab,i)=>{tab.classList.toggle('active',tab===t);panels[i].classList.toggle('active',tab===t);});});
  const slider=document.querySelector('.slider');if(slider){const slides=slider.querySelector('.slides');const items=slider.querySelectorAll('.slide');const dots=slider.querySelectorAll('.dot');let idx=0;function go(i){idx=(i+items.length)%items.length;slides.style.transform=`translateX(-${idx*100}%)`;dots.forEach((d,k)=>d.classList.toggle('active',k===idx));}dots.forEach((d,i)=>d.addEventListener('click',()=>go(i)));setInterval(()=>go(idx+1),3500);go(0);}
  const tog=document.querySelector('[data-toggle]');if(tog){const btns=tog.querySelectorAll('button');const priceEls=document.querySelectorAll('[data-price]');function setMode(mode){btns.forEach(b=>b.classList.toggle('active',b.getAttribute('data-mode')===mode));priceEls.forEach(el=>{const m=el.getAttribute('data-monthly');const y=el.getAttribute('data-yearly');el.textContent=mode==='yearly'?y:m;});}btns.forEach(b=>b.addEventListener('click',()=>setMode(b.getAttribute('data-mode'))));setMode('monthly');}
  document.addEventListener('submit',e=>{const f=e.target.closest('[data-ajax]');if(!f)return;e.preventDefault();alert('Thanks! (Demo)');f.reset();});
})();